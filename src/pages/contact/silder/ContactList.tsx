import {FC} from "react";
import {styled} from "umi";

const ContactList:FC = () => {
    return(
        <StyledContactList>ContactList</StyledContactList>
    )
}
export default ContactList
const StyledContactList = styled.div`
  flex: 1;
`
