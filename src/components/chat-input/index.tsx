import {FC} from "react";
import {styled} from "umi";

const ChatInput:FC = () => {
    return(
        <StyledChatInput contentEditable={true}>
           xxxx
        </StyledChatInput>
    )
}
const StyledChatInput = styled.div`
`
export default ChatInput
